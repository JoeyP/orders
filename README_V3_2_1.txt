B&L Neeley Orders V3.2.1 — Edit Popup Button Fix

Fixes:
- Save Changes works through a stable document-level handler.
- Cancel works through a stable document-level handler.
- X close button works through a stable document-level handler.
- Delete Order works through a stable document-level handler.
- Delete Order text is now visible (legacy CSS had a red !important text rule).
- Save Changes shows "Saving…" while the update is being processed.
- All pages are cache-busted to V3.2.1.

No Supabase/database migration is required for this update.
Keep config.js unchanged.
